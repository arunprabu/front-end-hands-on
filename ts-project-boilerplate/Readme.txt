Open the entire ts-project-boilerplate in new Editor Window 
open the terminal. run the following command 
  tsc --watch 

make changes in src/*.ts  and save.. see tsc compiling it on watch mode 
the compiled files would be part of public/dist/assets/scripts/ folder 

Open the public folder alone in another editor window 
  select the index.html .
  start the Live Server . 
------------

Add all you htmls inside public folder. 
Write the TS scripts inside src/ folder. 

And develop the app. 